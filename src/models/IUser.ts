export default interface IUser {
    username: string,
    name: string | null,
    lastname: string | null,
    surname: string | null,
    phone: string | null,
    gender: string | null,
    birthday: string | null,
}
